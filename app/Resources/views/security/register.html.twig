{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="control">
         <button class="button is-link" type="submit">Valider</button>
    </div>

    {{ form_end(form) }}

{% endblock %}